apiVersion: inference.networking.k8s.io/v1
kind: InferencePool
metadata:
  name: e2e-pool
  namespace: e2e-integration
spec:
  targetPorts:
    - number: 8000
  selector:
    matchLabels:
      app: llm-sim
  endpointPickerRef:
    name: epp-svc
    kind: Service
    port:
      number: 9002
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: epp
  namespace: e2e-integration
---
kind: Role
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: epp-pod-read
  namespace: e2e-integration
rules:
  - apiGroups: ["inference.networking.x-k8s.io"]
    resources: ["inferenceobjectives", "inferencemodelrewrites"]
    verbs: ["get", "watch", "list"]
  - apiGroups: ["inference.networking.k8s.io"]
    resources: ["inferencepools"]
    verbs: ["get", "watch", "list"]
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["get", "watch", "list"]
---
kind: RoleBinding
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: epp-pod-read-binding
  namespace: e2e-integration
subjects:
  - kind: ServiceAccount
    name: epp
    namespace: e2e-integration
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: epp-pod-read
---
kind: ClusterRole
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: epp-auth-reviewer
rules:
  - apiGroups: ["authentication.k8s.io"]
    resources: ["tokenreviews"]
    verbs: ["create"]
  - apiGroups: ["authorization.k8s.io"]
    resources: ["subjectaccessreviews"]
    verbs: ["create"]
---
kind: ClusterRoleBinding
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: epp-auth-reviewer-binding
subjects:
  - kind: ServiceAccount
    name: epp
    namespace: e2e-integration
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: epp-auth-reviewer
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: epp-plugins-config
  namespace: e2e-integration
data:
  default-plugins.yaml: |
    apiVersion: inference.networking.x-k8s.io/v1alpha1
    kind: EndpointPickerConfig
    featureGates:
    - flowControl
    plugins:
    - type: queue-scorer
    - type: kv-cache-utilization-scorer
    schedulingProfiles:
    - name: default
      plugins:
      - pluginRef: queue-scorer
      - pluginRef: kv-cache-utilization-scorer
    # maxRequests caps the EPP flow control admission queue so that
    # e2e tests can drive queue_size > 0 with a small number of
    # concurrent probe requests. In production this would be much
    # higher (or unlimited).
    flowControl:
      maxRequests: "2"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: epp
  namespace: e2e-integration
  labels:
    app: epp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: epp
  template:
    metadata:
      labels:
        app: epp
    spec:
      serviceAccountName: epp
      terminationGracePeriodSeconds: 10
      containers:
        - name: epp
          image: ${EPP_IMAGE}
          imagePullPolicy: Never
          args:
            - --pool-name
            - "e2e-pool"
            - --pool-namespace
            - "e2e-integration"
            - --v
            - "4"
            - --zap-encoder
            - "json"
            - --grpc-port
            - "9002"
            - --grpc-health-port
            - "9003"
            - --config-file
            - /config/default-plugins.yaml
            - --metrics-endpoint-auth=false
          env:
            # Fallback env var in case featureGates in config is not supported.
            - name: ENABLE_EXPERIMENTAL_FLOW_CONTROL_LAYER
              value: "true"
          ports:
            - containerPort: 9002
            - containerPort: 9003
            - name: metrics
              containerPort: 9090
          livenessProbe:
            grpc:
              port: 9003
              service: inference-extension
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            grpc:
              port: 9003
              service: inference-extension
            initialDelaySeconds: 5
            periodSeconds: 10
          volumeMounts:
            - name: plugins-config-volume
              mountPath: "/config"
      volumes:
        - name: plugins-config-volume
          configMap:
            name: epp-plugins-config
---
apiVersion: v1
kind: Service
metadata:
  name: epp-svc
  namespace: e2e-integration
spec:
  selector:
    app: epp
  ports:
    - name: grpc
      protocol: TCP
      port: 9002
      targetPort: 9002
      appProtocol: http2
    - name: metrics
      port: 9090
      targetPort: 9090
  type: ClusterIP
