\* Recycled names but a per-submission token on results -> the foreign result is
\* rejected. CorrectResponse HOLDS.
CONSTANTS
    Reqs = {r1, r2}
    MaxDup = 1
    Recycle = TRUE
    TokenGuard = TRUE
SPECIFICATION Spec
INVARIANT TypeOK
INVARIANT CorrectResponse
