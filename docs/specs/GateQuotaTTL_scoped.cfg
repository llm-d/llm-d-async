\* No whole-key expiry of a live counter (reclaim only genuinely-leaked slots) ->
\* the counter tracks real concurrency -> NoOverAdmission HOLDS.
CONSTANTS
    Workers = {w1, w2}
    Limit = 1
    BlindTTL = FALSE
SPECIFICATION Spec
INVARIANT TypeOK
INVARIANT NoOverAdmission
