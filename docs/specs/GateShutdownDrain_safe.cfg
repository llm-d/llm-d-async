\* Current code: flow.Shutdown() runs after wg.Wait() -> retry worker outlives
\* workers -> the bare retry sends are safe. Expected: all properties hold.
CONSTANTS
    Workers = {w1, w2}
    Backlog = 2
    SafeOrdering = TRUE
SPECIFICATION Spec
INVARIANT TypeOK
PROPERTY NoStuckWorker
PROPERTY AllBacklogFlushed
PROPERTY CleanShutdown
