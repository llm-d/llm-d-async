\* Current code: whole-key EXPIRE can fire while reserved holders are in flight ->
\* NoOverAdmission VIOLATED (a mid-flight expiry re-admits up to Limit again).
CONSTANTS
    Workers = {w1, w2}
    Limit = 1
    BlindTTL = TRUE
SPECIFICATION Spec
INVARIANT TypeOK
INVARIANT NoOverAdmission
