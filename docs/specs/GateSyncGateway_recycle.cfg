\* Recycled result-queue names, name-only correlation -> a late result on a recycled
\* name is returned to the WRONG caller. CorrectResponse VIOLATED.
CONSTANTS
    Reqs = {r1, r2}
    MaxDup = 1
    Recycle = TRUE
    TokenGuard = FALSE
SPECIFICATION Spec
INVARIANT TypeOK
INVARIANT CorrectResponse
